# packages/scorers

Scoring policy engines, deterministic/LLM-based scorers, F-beta calculators.

Includes:
- `metrics.py`: overlap and F-beta primitives
- `contracts.py`: scorer-facing response contract validation (blocking-only severity), strict competition gate helpers, and readable summary markdown
